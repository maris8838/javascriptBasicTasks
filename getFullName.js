/*
write a function called "getFullName" .
given a first and last names ,
 "getFullName" returns a single string with the
  given first and last names seperated by a single space.
*/
function getFullName (firstName,lastName) {
    console.log(firstName+" "+lastName)
}
getFullName("maris","ulaganathan");
